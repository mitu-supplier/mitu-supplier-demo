spring:
  profiles:
    active: dev
  application:
    name: forest-file-web

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: '*'